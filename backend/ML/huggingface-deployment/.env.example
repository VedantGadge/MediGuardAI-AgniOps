# MediGuardAI API - Environment Variables
# ==========================================
# IMPORTANT: This file is for LOCAL DEVELOPMENT ONLY
# DO NOT commit this file with actual values to Git!
# For Hugging Face deployment, add these as Secrets in Space Settings
# ==========================================

# === Authentication Configuration ===

# MongoDB Connection String
# Get this from MongoDB Atlas: Database -> Connect -> Connect your application
# Format: mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database Name (default: mediguard)
DB_NAME=mediguard

# JWT Secret Key (MUST be a strong random string)
# Generate with PowerShell: 
#   $bytes = New-Object byte[] 32
#   [Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($bytes)
#   [Convert]::ToBase64String($bytes)
# Or with Python:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your_very_long_random_secret_key_here_at_least_32_characters

# JWT Token Expiration (examples: 30d, 7d, 24h, 60m)
JWT_EXPIRE=30d

# === ML/AI API Keys ===

# Google Gemini API Key
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Groq API Key
# Get from: https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here

# === Application Settings ===

# Server Port (default: 7860 for Hugging Face)
PORT=7860

# Client URL for CORS (for local development, use http://localhost:3000)
CLIENT_URL=http://localhost:3000

# === Instructions ===

# 1. Copy this file to .env in the same directory
# 2. Replace all placeholder values with actual credentials
# 3. NEVER commit .env to Git (already in .gitignore)
# 4. For Hugging Face deployment:
#    - Go to your Space -> Settings -> Variables and secrets
#    - Add each variable as a secret (NOT as a variable)
# 5. For local development:
#    - Install dependencies: pip install -r requirements.txt
#    - Run: python app.py or uvicorn app:app --reload

# === Security Reminders ===

# ✅ Use strong, random values for JWT_SECRET
# ✅ Never share your .env file or commit it to version control
# ✅ Use different credentials for development and production
# ✅ Rotate keys regularly (especially JWT_SECRET)
# ✅ Whitelist IP 0.0.0.0/0 in MongoDB Atlas for Hugging Face deployment
# ✅ Use environment variables or secrets management in production
